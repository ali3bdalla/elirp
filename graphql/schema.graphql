type Query @guard {
#    me: User @auth
#    user(id: ID @eq): User @find
#    users(orderBy: _@orderBy(columns: ["created_at", "id"])): [User!]! @paginate
    accounts: [Company]! @all
}

#import models.graphql
#import actions.graphql

